// TrustSection removed — owner decision #8: no partner permissions.
// This component is intentionally empty so the homepage still renders
// without breaking, but shows no fake brand claims.
export default function TrustSection() {
  return null;
}
